# Copy this file to .env and fill in your own values
# NEVER commit the real .env file

# Telegram Bot token — get from @BotFather on Telegram
BOT_TOKEN=your_telegram_bot_token_here

# Your personal Telegram chat ID — the bot will ONLY talk to this ID
# Get it by messaging @userinfobot on Telegram
CHAT_ID=your_telegram_chat_id_here

# Google Sheets ID — the long string in your Sheet URL
# https://docs.google.com/spreadsheets/d/THIS_PART_HERE/edit
SHEET_ID=your_google_sheet_id_here

# Path to your Google service account credentials file
GOOGLE_CREDS=credentials.json

# Secret token for the /trigger/* cron endpoints — anyone who knows your
# server URL can call them without this. Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Then update your crontab lines to pass the header:
#   curl -s -X POST -H "X-Trigger-Secret: $TRIGGER_SECRET" http://localhost:8000/trigger/daily-recap
TRIGGER_SECRET=your_trigger_secret_here
